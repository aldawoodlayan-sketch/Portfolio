const menuButton=document.querySelector('.menu-button');const nav=document.querySelector('.nav-links');if(menuButton&&nav){menuButton.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}document.querySelectorAll('.image-wrap img').forEach(img=>{const wrap=img.closest('.image-wrap');const ok=()=>wrap&&wrap.classList.remove('missing');const bad=()=>wrap&&wrap.classList.add('missing');if(img.complete){img.naturalWidth?ok():bad()}img.addEventListener('load',ok);img.addEventListener('error',bad)});
